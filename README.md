# TYPESCRIPT Practice Hub
## Small projects build on typescript and js for studying purpose

## Structure of repo:
* **`01-book-app/`** - An application for enounter the books. Can be implemented for other projects as list of things. Features implemented:
 * Type Assertion (as HTML...Element), 
 * Operating of state of elements instead straight manipulition od DOM elements, 
 * Typescript config

* **`02-todo-app/`** - An application for writing your tasks. Can be implemented for other projects as list of things. Features implemented:
 * Type Assertion (as HTML...Element), 
 * Operating of state of elements instead straight manipulition od DOM elements
 * Hook useReducer as main function ot manage the global state
 * Deleting specific task by individual button per task
 * types as Type and Interface and other 

## Technologies:
* Typescript with 'strict mode';
* Helpfull frameworks as ..

## How to download projects on your pc?

```bash
   git clone [https://github.com/artemkril07/typescript-practice.git](https://github.com/artemkril07/typescript-practice.git)
